#include <iostream>
using namespace std;

int main() 
{
    int i;
    string nama[3];
    for(i=0;i<3;i++)
    {
    cout << "masukkan nama :";
    cin >> nama[i];
    }
    for(i=0;i<3;i++)
    {
    cout <<"nama anda adalah :";
    cout <<nama[i] <<endl;
    }
    return 0;
}
